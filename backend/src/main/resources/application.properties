spring.application.name=FinalProject

# Server Configuration
# server.baseUrl=${BASE_URL:http://localhost:8080}

# MySQL Database Configuration
spring.application.name=FinalProject

# Server Configuration

server.port=5000

# MySQL Database Configuration - Railway (Production) / Local (Development)
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/ebdb}
spring.datasource.username=${MYSQLUSER:admin}
spring.datasource.password=${MYSQLPASSWORD:Shubham12345}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.defer-datasource-initialization=${DEFER_DATA_INIT:true}
spring.sql.init.mode=always
# spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:true}
# spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.MySQL8Dialect}

# Error Handling
# server.error.include-message=${ERROR_INCLUDE_MESSAGE:always}
# server.error.include-binding-errors=${ERROR_INCLUDE_BINDING_ERRORS:always}
# server.error.include-stacktrace=${ERROR_INCLUDE_STACKTRACE:never}

# # Logging Configuration
# logging.level.org.springframework.security=${LOG_SECURITY_LEVEL:DEBUG}
# logging.level.org.springframework.web=${LOG_WEB_LEVEL:DEBUG}
# logging.level.org.hibernate.SQL=${LOG_SQL_LEVEL:DEBUG}

# CORS Configuration
spring.mvc.cors.allowed-origins=http://shopping-platform.us-east-2.elasticbeanstalk.com,https://shopping-platform.us-east-2.elasticbeanstalk.com,http://www.shopping-platform.site,https://www.shopping-platform.site,http://shopping-platform.site,https://shopping-platform.site,http://localhost:4200,https://localhost:4200
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*

# Security Headers for HTTPS
server.ssl.enabled=${SSL_ENABLED:false}
security.headers.frame-options=${FRAME_OPTIONS:DENY}
security.headers.content-type-options=${CONTENT_TYPE_OPTIONS:nosniff}

# Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SJVh94ayqM9QeO}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:zNF7vdiS2vaEqTO9TBffJJLe}
razorpay.currency=${RAZORPAY_CURRENCY:INR}
razorpay.company.name=${RAZORPAY_COMPANY:Cara}
razorpay.test.mode=${RAZORPAY_TEST_MODE:true}

# File Upload Configuration
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}

# Active Profile
# spring.profiles.active=${ACTIVE_PROFILE:dev}

# OpenAI Configuration
openai.api.key=${OPENAI_API_KEY:AIzaSyB6pZxDO4u9JN2uf0tF5fp-WJNu-74T9iU}